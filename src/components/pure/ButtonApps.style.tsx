import { styled } from "styled-components";

export const ButtonAppsCss = styled.button`
    display: flex;
    align-items: center;
    padding: 0.5rem 1rem;
    border-radius: .5rem;
    gap: 0.5rem;
    background: var(--box-shadow-inputs);
`;