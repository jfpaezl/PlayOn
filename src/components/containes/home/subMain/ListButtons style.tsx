import {styled} from "styled-components";

export const ListButtonsCss = styled.div`
    display: flex;
    gap: 1rem;
    margin: auto;
    overflow-wrap: break-word;
    .icon{
        font-size: 1.2rem;
    }

`;